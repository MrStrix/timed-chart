apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "timed.fullname" . }}-test-connection"
  labels:
    {{- include "timed.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "timed.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
